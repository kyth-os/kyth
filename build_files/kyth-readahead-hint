#!/usr/bin/env bash
# kyth-readahead-hint — apply gaming 2048 else 512
set -euo pipefail
if [[ -f /run/kyth/gaming-hint ]]; then
  kb=2048
else
  kb=512
fi
for f in /sys/block/nvme*/queue/read_ahead_kb; do
  [[ -f "$f" ]] || continue
  echo "$kb" > "$f" 2>/dev/null || true
done
