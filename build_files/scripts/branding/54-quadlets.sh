# shellcheck shell=bash
# ── Quadlet gaming services ──────────────────────────────────────────────
# podman quadlet .container units hash-gated, offline
mkdir -p /etc/containers/systemd 2>/dev/null || true
