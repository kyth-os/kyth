# shellcheck shell=bash
# ── Signing preset (gpg/cosign) ──────────────────────────────────────────
# signing.toml hash-gated, gitsign/cosign keys offline
# git config commit.gpgsign + cosign private.key deployed via Hub Work
