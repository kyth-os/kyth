# shellcheck shell=bash
# ── Per-game overlay MangoHud+vkBasalt ───────────────────────────────────
# overlay.toml hash-gated, MangoHud.conf.d + vkBasalt.conf per gaming-per-game
mkdir -p /etc/MangoHud 2>/dev/null || true
mkdir -p /etc/vkBasalt 2>/dev/null || true
