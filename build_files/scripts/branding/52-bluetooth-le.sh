# shellcheck shell=bash
# ── Bluetooth LE Audio preset ────────────────────────────────────────────
# wireplumber bluetooth.lua.d drop-in hash-gated, offline
mkdir -p /etc/wireplumber/bluetooth.lua.d 2>/dev/null || true
