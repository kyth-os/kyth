#!/usr/bin/env python3
"""kyth-driver-switch — gpu driver nvidia/open/amd + mesa_git per driver.toml (offline)."""
from kyth_shared.driver_preset import load_driver
from kyth_shared.commands import run
def main():
    cfg=load_driver()
    print(f"kyth-driver-switch: gpu {cfg['gpu']} mesa_git {cfg['mesa_git']}")
    # wraps thirdparty repos.json already; here just log, real switch via thirdparty.sh --driver
    return 0
if __name__=="__main__":
    import sys; sys.exit(main())
