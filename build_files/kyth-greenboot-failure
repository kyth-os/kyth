#!/usr/bin/env bash
# Count one failure per boot ID and quarantine repeatedly unhealthy digests.
set -euo pipefail

exec /usr/bin/kyth-boot-health record-failure --reason "greenboot required check failed"
